# JarPacker
утилита быстрой для компоновки .jar файлов (fabric mod) в один .jar файл.

## Использование
```bash
.\JarPacker.exe jar=my_jar.jar input=input.jar --debug
```

на выходе получите input.jar-patch.jar
в котором будет находится my_jar.jar.
